# Smoke evidence (local + interim HTTPS)

Generated by:

- `npm run smoke:company-plugins-api` — boots/reuses local Worker, curated sync
- `npm run smoke:company-store-live` — strongest live-origin Market checks
  (packages/meta/installMethods, `q`, `/api/v1/plugins/search` pagination, pin refuse)
- `npm run verify:company-store-adapter-live` — feeds live JSON through desktop
  `createDsh1024StyleStoreAdapter` (Stage 2 parser; no production pin)

Artifacts:

- `LATEST.md` — human-readable summary (local M1 + interim + live smoke)
- `LIVE.md` — latest `smoke:company-store-live` run
- `plugins-api-*.json` — optional captured response bodies
- `interim-https-*.json` / `recheck-*` — earlier manual tunnel rechecks
- `live-smoke-*.json` / `adapter-live-*.json` — scripted live verification

**`durable: false` / `pinAllowed: false`** on trycloudflare — not a company apex.
Do not pin AI Buddy `company-store` constants to the trycloudflare hostname.

Durable public HTTPS URLs and the secrets-gated deploy workflow belong in
`docs/company-fork-deploy.md` once Cloudflare credentials exist.
