# Smoke evidence (local listening origin)

Generated by `npm run smoke:company-plugins-api`.

- `LATEST.md` — human-readable acceptance summary (committed).
- `plugins-api-*.json` — optional captured response bodies (may be gitignored if large).

Public HTTPS URLs belong in `docs/company-fork-deploy.md` once Cloudflare credentials exist.
